using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JetStarGameObject : MonoBehaviour
{
    public JetStar jetStar;
    // Start is called before the first frame update
    void Start()
    {
        jetStar = new JetStar();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
