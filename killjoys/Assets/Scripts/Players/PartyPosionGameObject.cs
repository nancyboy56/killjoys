using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PartyPosionGameObject : MonoBehaviour
{
    public PartyPosion partyPosion;
    // Start is called before the first frame update
    void Start()
    {
        partyPosion = new PartyPosion();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
